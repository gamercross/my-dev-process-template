# 작업 로그

이 문서는 "왜 지금 이 모양이 됐는지"를 날짜순으로 기록하는 곳입니다.
`docs/`의 다른 문서들이 **결과**(최종 규칙·방법론)만 담는다면, 여기는
**과정**(어떤 시행착오·발견·결정이 있었는지)을 담습니다 — 새 항목을
저장하거나, 회고에서 뭔가 배우거나, 시스템 자체를 고칠 때마다 여기에 한
문단씩 추가하세요.

이 저장소가 어떻게 지금 구조(7단계, gates.md, scope-tiers.md, 트랙 자동
인식 등)에 이르렀는지의 원본 기록은 `main` 브랜치의 worklog.md에 있습니다.
이 `template` 브랜치는 그 시스템을 그대로 물려받되, 여러분 자신의 기록은
여기서부터 새로 시작합니다.

---

(다음 항목부터 날짜별로 추가하세요. 형식 예: `## YYYY-MM-DD` 아래 `### 제목`)
